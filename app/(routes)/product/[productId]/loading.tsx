// app/products/loading.tsx (or wherever your route is)
import ProductCardSkeleton from "@/components/ProductCardSkeleton";

// update path as needed

const Loading = () => {
  return <ProductCardSkeleton />;
};

export default Loading;
